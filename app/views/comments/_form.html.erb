<%= form_with(model: comment, class: "mt-4") do |form| %>
  <% if comment.errors.any? %>
    <div class="alert alert-danger">
      <h4><%= pluralize(comment.errors.count, "error") %> prohibited this comment from being saved:</h4>

      <ul>
        <% comment.errors.full_messages.each do |error| %>
          <li><%= error %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.hidden_field :post_id %>

  <div class="form-group">
    <%= form.label :content, "Write your comment:"%>
    <%= form.text_area :content, class: "form-control" , placeholder: "Remember to be respectful!"  %>
  </div>

  <div class="form-group">
    <%= form.submit "Submit", class: "btn btn-primary" %>
  </div>
<% end %>
